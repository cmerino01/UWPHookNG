<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Brushes that consume the active palette (Palette.Dark.xaml or Palette.Light.xaml).
        ThemeManager swaps the palette dictionary at runtime; because these brushes use
        DynamicResource on the color keys, the swap is live.
    -->

    <!-- Surfaces -->
    <SolidColorBrush x:Key="BgBrush"               Color="{DynamicResource BgColor}" />
    <SolidColorBrush x:Key="SurfaceBrush"          Color="{DynamicResource SurfaceColor}" />
    <SolidColorBrush x:Key="SurfaceElevatedBrush"  Color="{DynamicResource SurfaceElevatedColor}" />
    <SolidColorBrush x:Key="SurfaceHoverBrush"     Color="{DynamicResource SurfaceHoverColor}" />
    <SolidColorBrush x:Key="BorderBrush"           Color="{DynamicResource BorderColor}" />
    <SolidColorBrush x:Key="BorderStrongBrush"     Color="{DynamicResource BorderStrongColor}" />

    <!-- Accent -->
    <SolidColorBrush x:Key="AccentBrush"           Color="{DynamicResource AccentColor}" />
    <SolidColorBrush x:Key="AccentHoverBrush"      Color="{DynamicResource AccentHoverColor}" />
    <SolidColorBrush x:Key="AccentPressedBrush"    Color="{DynamicResource AccentPressedColor}" />

    <!-- Text -->
    <SolidColorBrush x:Key="TextPrimaryBrush"      Color="{DynamicResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush"    Color="{DynamicResource TextSecondaryColor}" />
    <SolidColorBrush x:Key="TextDisabledBrush"     Color="{DynamicResource TextDisabledColor}" />
    <SolidColorBrush x:Key="TextOnAccentBrush"     Color="{DynamicResource TextOnAccentColor}" />

    <!-- Selection glow -->
    <SolidColorBrush x:Key="SelectionGlowBrush"    Color="{DynamicResource SelectionGlowColor}" />

    <!-- Typography. Segoe UI Variable Display ships on Win11; Segoe UI is the Win10 fallback. -->
    <FontFamily x:Key="DisplayFont">Segoe UI Variable Display, Segoe UI, Arial</FontFamily>
    <FontFamily x:Key="UiFont">Segoe UI Variable Text, Segoe UI, Arial</FontFamily>

    <!-- Geometry -->
    <CornerRadius x:Key="CardRadius">10</CornerRadius>
    <CornerRadius x:Key="ChipRadius">999</CornerRadius>
    <Thickness    x:Key="CardPadding">16</Thickness>

</ResourceDictionary>
